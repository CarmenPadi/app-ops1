pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/CarmenPadi/app-ops1.git', branch: 'feature/new-feature'
            }
        }
    }
}
